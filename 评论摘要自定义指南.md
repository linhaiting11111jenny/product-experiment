# 评论摘要自定义指南

本指南详细说明如何根据不同实验条件自定义 `index.html` 中的评论摘要部分。

## 📍 修改位置

评论摘要的显示逻辑在 `index.html` 文件的 **`initSummary()` 函数**中（约第400-470行）。

## 🎯 三种修改方式

### 方式1：修改现有条件的内容

直接修改对应条件内的文本或HTML结构。

**示例：修改 rating 条件的星级**

```javascript
if (condition === 'rating') {
    summaryContent.innerHTML = `
        <div class="rating-summary">
            <div class="stars">★★★★★</div>  <!-- 改为5星 -->
            <div class="rating-count">共 200 条评论</div>  <!-- 修改评论数量 -->
        </div>
    `;
}
```

**示例：修改 AIGRS 文本内容**

```javascript
else if (condition === 'aigrs_positive') {
    summaryContent.innerHTML = `
        <div class="aigrs-text">
            这是你自定义的正向评价文本。可以根据研究需要修改内容。
        </div>
    `;
}
```

### 方式2：添加新的实验条件

在 `initSummary()` 函数中添加新的 `else if` 分支。

**步骤：**

1. **在 `initSummary()` 函数中添加新条件**

```javascript
else if (condition === 'your_new_condition') {
    summaryContent.innerHTML = `
        <div class="your-custom-class">
            <!-- 你的自定义内容 -->
        </div>
    `;
}
```

2. **在 CSS 部分添加对应的样式类**（如果需要）

在 `<style>` 标签中添加样式（约第104行附近）：

```css
.your-custom-class {
    color: #333;
    font-size: 14px;
    line-height: 1.8;
    /* 添加你需要的样式 */
}
```

3. **在 URL 中使用新条件**

```
index.html?condition=your_new_condition
```

### 方式3：修改样式和布局

修改 CSS 样式来改变评论摘要的外观。

**位置：** `<style>` 标签中（约第73-108行）

**可修改的样式类：**

- `.summary-section` - 整个摘要区域的样式
- `.summary-title` - "用户评论摘要"标题的样式
- `.rating-summary` - 星级评分的布局样式
- `.stars` - 星级的样式
- `.rating-count` - 评论数量的样式
- `.aigrs-text` - AIGRS文本的样式

## 📝 完整示例

### 示例1：添加带图标的评分条件

**1. 在 `initSummary()` 中添加：**

```javascript
else if (condition === 'rating_with_icon') {
    summaryContent.innerHTML = `
        <div class="rating-with-icon">
            <div class="icon">⭐</div>
            <div class="rating-info">
                <div class="stars">★★★★☆</div>
                <div class="rating-count">156条评论</div>
            </div>
        </div>
    `;
}
```

**2. 在 CSS 中添加样式：**

```css
.rating-with-icon {
    display: flex;
    align-items: center;
    gap: 15px;
}

.rating-with-icon .icon {
    font-size: 24px;
}

.rating-with-icon .rating-info {
    display: flex;
    flex-direction: column;
    gap: 5px;
}
```

### 示例2：添加多段落AIGRS文本

```javascript
else if (condition === 'aigrs_detailed') {
    summaryContent.innerHTML = `
        <div class="aigrs-detailed">
            <p class="aigrs-paragraph">
                用户评论显示，该产品在音质、续航和舒适度方面表现稳定。
            </p>
            <p class="aigrs-paragraph">
                部分用户对降噪功能有不同看法，整体评价较为均衡。
            </p>
        </div>
    `;
}
```

```css
.aigrs-detailed {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.aigrs-paragraph {
    margin: 0;
    color: #333;
    font-size: 14px;
    line-height: 1.8;
}
```

### 示例3：添加带统计数据的条件

```javascript
else if (condition === 'statistics') {
    summaryContent.innerHTML = `
        <div class="statistics-summary">
            <div class="stat-item">
                <span class="stat-label">好评率：</span>
                <span class="stat-value">85%</span>
            </div>
            <div class="stat-item">
                <span class="stat-label">平均评分：</span>
                <span class="stat-value">4.3/5</span>
            </div>
            <div class="stat-item">
                <span class="stat-label">评论总数：</span>
                <span class="stat-value">156条</span>
            </div>
        </div>
    `;
}
```

```css
.statistics-summary {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.stat-item {
    display: flex;
    justify-content: space-between;
    font-size: 14px;
}

.stat-label {
    color: #666;
}

.stat-value {
    color: #333;
    font-weight: 500;
}
```

## 🎨 设计建议

### 保持一致性

- 所有条件使用相同的背景色和基本布局（`.summary-section`）
- 保持字体大小和行高的一致性
- 使用相同的颜色方案（灰色系，避免过度强调）

### 中性原则

- 避免使用过于鲜艳的颜色
- 不要添加动画或闪烁效果
- 保持文本客观中立

### 响应式设计

- 使用 `flex` 布局以适应不同屏幕
- 考虑移动端的显示效果
- 使用相对单位（如 `rem`、`%`）而非固定像素

## 🔧 测试方法

### 1. 本地测试

在浏览器中打开不同条件的URL：

```
index.html?condition=rating
index.html?condition=aigrs_neutral
index.html?condition=aigrs_positive
index.html?condition=your_new_condition  # 测试新条件
```

### 2. 检查显示效果

- 检查不同条件是否正确显示
- 检查样式是否正确应用
- 检查在不同浏览器中的显示效果
- 检查移动端的显示效果

### 3. 验证数据记录

确保修改后，`condition` 参数仍然正确记录在实验数据中：

```javascript
// 在浏览器控制台执行
const data = JSON.parse(localStorage.getItem('experimentData'));
console.log(data.condition);  // 应该显示对应的条件值
```

## ⚠️ 注意事项

1. **保持条件值唯一**：每个实验条件应该有唯一的标识符
2. **更新文档**：如果添加新条件，记得更新 `Credamo配置指南.md` 中的链接配置
3. **测试所有条件**：确保所有条件都能正常显示
4. **保持代码整洁**：添加注释说明每个条件的用途
5. **不要删除默认条件**：除非确定不需要，否则保留 `else` 分支作为默认情况

## 📚 相关代码位置

- **HTML结构**：第251-257行（`.summary-section`）
- **CSS样式**：第73-108行（样式定义）
- **JavaScript逻辑**：第400-470行（`initSummary()` 函数）
- **条件获取**：第388行（从URL参数获取 `condition`）

## 💡 快速参考

**修改文本内容**：直接修改 `innerHTML` 中的文本

**修改样式**：在 `<style>` 标签中添加或修改CSS类

**添加新条件**：
1. 在 `initSummary()` 中添加 `else if` 分支
2. 在CSS中添加对应的样式类（如需要）
3. 在URL中使用新条件：`?condition=your_new_condition`

**测试**：使用不同条件的URL在浏览器中测试

